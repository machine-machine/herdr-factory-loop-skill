<!--
.m2herd/NOTES.md — the central notes file of the m2herd context fabric. The machineroom pane
live-views this file (`watch -n 2 -t cat .m2herd/NOTES.md`).

Append entries with:   m2herd.sh note "text"        → "- [<UTC ts>] text" lands below the marker
Refile them with:      m2herd.sh refile --area A    → moves everything below the marker into
                                                      .m2herd/context/A/context.md and resets
                                                      this file back to the template.

Everything ABOVE the marker is template boilerplate; everything BELOW is live content.
-->

<!-- === M2HERD:LIVE === -->
