<!--
.m2herd/evolver/LESSONS.md — accepted factory lessons. Populated by `m2herd.sh evolve apply`
for proposals of kind memory/policy/template (never repo).

Read them back with:  m2herd.sh resume        → shows the last 5 lines below the marker as
                                                "Recent factory lessons:"
Applied automatically: m2herd-up dispatch/collect worker prompts get one extra pointer
                       sentence at this file's path when it has content below the marker.

Everything ABOVE the marker is template boilerplate; everything BELOW is live, accepted lessons.
Line format: - [<UTC ts>] (<proposal-id>) <lesson text>
-->

<!-- === M2HERD:LIVE === -->
